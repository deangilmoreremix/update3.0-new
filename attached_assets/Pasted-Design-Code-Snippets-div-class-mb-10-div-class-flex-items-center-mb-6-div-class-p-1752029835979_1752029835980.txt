Design Code Snippets:


<div class="mb-10">
  <div class="flex items-center mb-6">
    <div class="p-2 bg-gradient-to-r from-orange-500 to-red-500 rounded-xl mr-3">
      <!-- Icon -->
    </div>
    <div>
      <h2 class="text-2xl font-semibold ${isDark ? 'text-white' : 'text-gray-900'}">Activities & Communications</h2>
      <p class="text-sm ${isDark ? 'text-gray-400' : 'text-gray-600'}">
        Manage your tasks, appointments, and communications
      </p>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-8">
    <div class="lg:col-span-2">
      <div class="${isDark ? 'bg-white/5 border-white/10' : 'bg-white border-gray-200'} backdrop-blur-xl border rounded-2xl p-6 mb-6">
        <h3 class="text-lg font-semibold ${isDark ? 'text-white' : 'text-gray-900'} mb-6 flex items-center">
          <!-- Icon -->
          Tasks & Activities
        </h3>
        <!-- TasksSection -->
      </div>
      
      <div class="mb-6">
        <!-- InteractionHistory -->
      </div>
    </div>
    
    <div class="space-y-6 lg:col-span-1">
      <div class="${isDark ? 'bg-white/5 border-white/10' : 'bg-white border-gray-200'} backdrop-blur-xl border rounded-2xl overflow-hidden">
        <div class="p-4 border-b ${isDark ? 'border-white/10' : 'border-gray-200'} flex justify-between items-center">
          <h3 class="font-semibold flex items-center ${isDark ? 'text-white' : 'text-gray-900'}">
            <!-- Icon -->
            Upcoming Appointments
          </h3>
        </div>
        <!-- AppointmentWidget -->
      </div>
      
      <div class="${isDark ? 'bg-white/5 border-white/10' : 'bg-white border-gray-200'} backdrop-blur-xl border rounded-2xl overflow-hidden">
        <div class="p-4 border-b ${isDark ? 'border-white/10' : 'border-gray-200'} flex justify-between items-center">
          <h3 class="font-semibold flex items-center ${isDark ? 'text-white' : 'text-gray-900'}">
            <!-- Icon -->
            Communications
          </h3>
        </div>
        <div class="p-4">
          <div class="space-y-3">
            <div class="p-3 rounded-lg ${isDark ? 'bg-white/5 hover:bg-white/10' : 'bg-gray-50 hover:bg-gray-100'} transition-colors cursor-pointer">
              <div class="flex items-center justify-between">
                <div class="flex items-center space-x-3">
                  <div class="p-2 rounded-lg ${isDark ? 'bg-blue-500/20 text-blue-400' : 'bg-blue-100 text-blue-600'}">
                    <!-- Icon -->
                  </div>
                  <div>
                    <p class="text-sm font-medium ${isDark ? 'text-white' : 'text-gray-900'}">Email Follow-up</p>
                    <p class="text-xs ${isDark ? 'text-gray-400' : 'text-gray-600'}">Microsoft deal</p>
                  </div>
                </div>
                <span class="text-xs ${isDark ? 'text-gray-400' : 'text-gray-500'}">Today</span>
              </div>
            </div>
            
            <div class="p-3 rounded-lg ${isDark ? 'bg-white/5 hover:bg-white/10' : 'bg-gray-50 hover:bg-gray-100'} transition-colors cursor-pointer">
              <div class="flex items-center justify-between">
                <div class="flex items-center space-x-3">
                  <div class="p-2 rounded-lg ${isDark ? 'bg-green-500/20 text-green-400' : 'bg-green-100 text-green-600'}">
                    <!-- Icon -->
                  </div>
                  <div>
                    <p class="text-sm font-medium ${isDark ? 'text-white' : 'text-gray-900'}">Meeting Notes</p>
                    <p class="text-xs ${isDark ? 'text-gray-400' : 'text-gray-600'}">Ford collaboration</p>
                  </div>
                </div>
                <span class="text-xs ${isDark ? 'text-gray-400' : 'text-gray-500'}">Yesterday</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <div class="mb-6">
      <!-- TasksAndFunnel -->
    </div>
    
    <div class="mb-6">
      <!-- RecentActivity -->
    </div>
</div>
src/components/dashboard/InteractionHistory.tsx
This component uses a grid for its cards, with dynamic background and text colors, and subtle hover animations.

Design Code Snippets:


<div class="${isDark ? 'bg-white/5 border-white/10' : 'bg-white border-gray-200'} backdrop-blur-xl border rounded-2xl p-6 mb-6">
  <div class="flex items-center justify-between mb-6">
    <h2 class="text-xl font-semibold ${isDark ? 'text-white' : 'text-gray-900'}">Interaction History</h2>
    <div class="flex items-center space-x-2">
      <button class="p-2 ${isDark ? 'text-gray-400 hover:bg-white/10' : 'text-gray-600 hover:bg-gray-100'} rounded-lg transition-colors">
        <!-- Icon -->
      </button>
      <button class="p-2 ${isDark ? 'text-gray-400 hover:bg-white/10' : 'text-gray-600 hover:bg-gray-100'} rounded-lg transition-colors">
        <!-- Icon -->
      </button>
    </div>
  </div>
  
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    <!-- Loop through interactions -->
    <div class="${interaction.bgColor} ${interaction.textColor} rounded-2xl p-5 hover:scale-105 transition-all duration-300 shadow-lg hover:shadow-xl">
      <div class="flex items-center justify-between mb-4">
        <span class="text-sm font-medium opacity-90">{interaction.date}</span>
        <button class="p-1.5 rounded-lg transition-colors ${
          interaction.textColor === 'text-white' ? 'hover:bg-white/20' : 'hover:bg-black/10'
        }">
          <!-- Icon -->
        </button>
      </div>
      
      <div class="mb-6">
        <h3 class="font-semibold text-sm mb-3 leading-tight">{interaction.type}</h3>
        <p class="text-2xl font-bold">{interaction.value}</p>
      </div>
      
      <div class="flex items-center justify-between">
        <div class="flex -space-x-2">
          <!-- Loop through participants -->
          <div class="relative">
            <!-- Avatar component -->
            <img class="border-3 border-white object-cover shadow-lg ring-2 ring-white/50" />
          </div>
          <!-- Remaining count -->
          <div class="w-8 h-8 rounded-full border-3 border-white bg-gray-400 flex items-center justify-center shadow-lg ring-2 ring-white/50">
            <span class="text-xs font-semibold text-white">+{interaction.participantIds.length - 3}</span>
          </div>
        </div>
        <!-- Icon -->
      </div>
    </div>
  </div>
</div>
src/components/TasksSection.tsx
This component primarily uses the TaskCard component for its visual representation.

Design Code Snippets:


<div class="mb-8">
  <div class="flex items-center justify-between mb-6">
    <div>
      <h2 class="text-xl font-semibold ${isDark ? 'text-white' : 'text-gray-900'}">Your Days Tasks</h2>
      <p class="text-sm ${isDark ? 'text-gray-400' : 'text-gray-600'}">{tasks.length} active tasks</p>
    </div>
    <span class="text-sm ${isDark ? 'text-gray-400' : 'text-gray-600'}">
      {tasks.filter(task => task.priority === 'high').length} high priority
    </span>
  </div>
  
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    <!-- Loop through tasks -->
    <!-- TaskCard component -->
  </div>
</div>
src/components/TaskCard.tsx (Used by TasksSection)
This component defines the styling for individual task cards.

Design Code Snippets:


<div class="backdrop-blur-xl border rounded-2xl p-6 hover:${isDark ? 'bg-gray-800/70' : 'bg-gray-50'} transition-all duration-300 group ${getCardClass(task.isHighlighted)}">
  <div class="flex items-start justify-between mb-4">
    <div class="flex items-center space-x-3">
      <!-- Avatar component -->
      <div>
        <h3 class="font-medium ${isDark ? 'text-white' : 'text-gray-900'}">
          {task.assignee?.name || `Team Member ${index + 1}`}
        </h3>
        <p class="text-xs ${isDark ? 'text-gray-400' : 'text-gray-600'}">
          {task.assignee?.role || 'Sales Representative'}
        </p>
      </div>
    </div>
    <button class="${isDark ? 'text-gray-400 hover:text-white' : 'text-gray-400 hover:text-gray-600'} transition-colors opacity-0 group-hover:opacity-100">
      <!-- Icon -->
    </button>
  </div>
  
  <div class="space-y-4">
    <div class="flex items-center space-x-3">
      <div class="p-2 rounded-lg ${isDark ? 'bg-white/10' : 'bg-gray-100'} ${getIconColor(task.type)}">
        <!-- Icon -->
      </div>
      <div>
        <h4 class="font-medium ${isDark ? 'text-white group-hover:text-green-400' : 'text-gray-900 group-hover:text-green-600'} transition-colors">
          {task.title}
        </h4>
        <p class="text-sm ${isDark ? 'text-gray-400' : 'text-gray-600'}">{task.subtitle}</p>
      </div>
    </div>
    
    <div class="flex items-center justify-between">
      <div class="flex items-center space-x-2">
        <span class="px-3 py-1 rounded-full text-xs ${isDark ? 'bg-white/10' : 'bg-gray-100'} ${getIconColor(task.type)}">
          {task.status}
        </span>
        <span class="px-2 py-1 rounded-full text-xs font-medium ${
          task.priority === 'high' ? (isDark ? 'bg-red-500/20 text-red-400' : 'bg-red-100 text-red-800') :
          task.priority === 'medium' ? (isDark ? 'bg-yellow-500/20 text-yellow-400' : 'bg-yellow-100 text-yellow-800') :
          (isDark ? 'bg-green-500/20 text-green-400' : 'bg-green-100 text-green-800')
        }">
          {task.priority}
        </span>
      </div>
      <div class="flex space-x-2">
        <button class="p-2 ${isDark ? 'bg-white/10 hover:bg-green-400/20' : 'bg-gray-100 hover:bg-green-100'} rounded-lg transition-colors">
          <!-- Icon -->
        </button>
        <button class="p-2 ${isDark ? 'bg-white/10 hover:bg-green-400/20' : 'bg-gray-100 hover:bg-green-100'} rounded-lg transition-colors">
          <!-- Icon -->
        </button>
      </div>
    </div>
  </div>
</div>
src/components/dashboard/TasksAndFunnel.tsx
This component features a calendar-like task schedule and a sales funnel visualization with progress bars.

Design Code Snippets:


<div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-8">
  <!-- Tasks Schedule -->
  <div class="${isDark ? 'bg-white/5 border-white/10' : 'bg-white border-gray-200'} backdrop-blur-xl border rounded-2xl p-6">
    <div class="flex items-center justify-between mb-6">
      <h3 class="text-lg font-semibold ${isDark ? 'text-white' : 'text-gray-900'}">Tasks Schedule</h3>
      <div class="flex space-x-2">
        <button class="p-2 ${isDark ? 'text-gray-400 hover:bg-white/10' : 'text-gray-600 hover:bg-gray-100'} rounded-lg transition-colors">
          <!-- Icon -->
        </button>
        <button class="p-2 ${isDark ? 'text-gray-400 hover:bg-white/10' : 'text-gray-600 hover:bg-gray-100'} rounded-lg transition-colors">
          <!-- Icon -->
        </button>
        <button class="p-2 ${isDark ? 'text-gray-400 hover:bg-white/10' : 'text-gray-600 hover:bg-gray-100'} rounded-lg transition-colors">
          <!-- Icon -->
        </button>
      </div>
    </div>

    <div class="mb-4">
      <h4 class="text-2xl font-bold ${isDark ? 'text-white' : 'text-gray-900'} mb-2">October</h4>
      <div class="grid grid-cols-7 gap-1 text-center text-sm mb-2">
        <!-- Day headers -->
        <div class="font-medium ${isDark ? 'text-gray-400' : 'text-gray-500'}">S</div>
        <!-- ... other days -->
      </div>
      <div class="grid grid-cols-7 gap-1 text-center text-sm">
        <!-- Loop through calendar days -->
        <!-- CalendarDay component -->
      </div>
    </div>

    <div class="space-y-3">
      <!-- Loop through task data -->
      <div class="flex items-center justify-between ${isDark ? 'bg-white/5 hover:bg-white/10' : 'bg-gray-50 hover:bg-gray-100'} p-3 rounded-lg transition-colors">
        <div class="flex items-center space-x-3">
          <div class="font-medium ${isDark ? 'text-white' : 'text-gray-700'}">{task.day}</div>
          <!-- TaskAssignees component -->
        </div>
        <div class="flex items-center space-x-2">
          <span class="text-sm ${isDark ? 'text-gray-400' : 'text-gray-600'}">{task.tasks} tasks</span>
          <button class="p-1 ${isDark ? 'text-gray-400 hover:text-gray-200 hover:bg-white/10' : 'text-gray-400 hover:text-gray-600 hover:bg-gray-200'} rounded transition-colors">
            <!-- Icon -->
          </button>
        </div>
      </div>
    </div>

    <!-- Selected Day Details -->
    <div class="mt-4 p-4 ${isDark ? 'bg-blue-500/10 border-blue-500/20' : 'bg-blue-50 border-blue-200'} rounded-lg border">
      <div class="flex items-center justify-between mb-2">
        <h5 class="font-semibold ${isDark ? 'text-blue-300' : 'text-blue-800'}">October {selectedDay}</h5>
        <button class="p-1 ${isDark ? 'text-blue-400 hover:bg-blue-500/20' : 'text-blue-600 hover:bg-blue-100'} rounded">
          <!-- Icon -->
        </button>
      </div>
      <p class="text-sm ${isDark ? 'text-blue-300' : 'text-blue-700'}">
        {/* Text content */}
      </p>
      <div class="mt-2 flex items-center space-x-2">
        <!-- TaskAssignees component -->
      </div>
    </div>
  </div>

  <!-- Stage Funnel -->
  <div class="${isDark ? 'bg-white/5 border-white/10' : 'bg-white border-gray-200'} backdrop-blur-xl border rounded-2xl p-6">
    <div class="flex items-center justify-between mb-6">
      <h3 class="text-lg font-semibold ${isDark ? 'text-white' : 'text-gray-900'}">Stage Funnel</h3>
      <div class="flex space-x-2">
        <button class="p-2 ${isDark ? 'text-gray-400 hover:bg-white/10' : 'text-gray-600 hover:bg-gray-100'} rounded-lg transition-colors">
          <!-- Icon -->
        </button>
        <button class="p-2 ${isDark ? 'text-gray-400 hover:bg-white/10' : 'text-gray-600 hover:bg-gray-100'} rounded-lg transition-colors">
          <!-- Icon -->
        </button>
      </div>
    </div>

    <div class="space-y-4">
      <!-- Loop through funnel data -->
      <div class="space-y-2">
        <div class="flex items-center justify-between">
          <span class="text-sm font-medium ${isDark ? 'text-gray-300' : 'text-gray-700'}">{stage.stage}</span>
          <span class="text-sm font-semibold ${isDark ? 'text-white' : 'text-gray-900'}">{stage.value}</span>
        </div>
        <div class="w-full ${isDark ? 'bg-gray-700' : 'bg-gray-200'} rounded-full h-2">
          <div class="${stage.color} ${isDark ? 'opacity-80' : 'opacity-100'} h-2 rounded-full transition-all duration-300" style="width: ${(4 - index) * 25}%"></div>
        </div>
      </div>
    </div>

    <div class="mt-6 p-4 ${isDark ? 'bg-white/5' : 'bg-gray-50'} rounded-lg">
      <div class="flex items-center justify-between">
        <span class="text-sm font-medium ${isDark ? 'text-gray-300' : 'text-gray-700'}">Weighted</span>
        <span class="text-sm font-medium ${isDark ? 'text-gray-300' : 'text-gray-700'}">Total</span>
      </div>
    </div>

    <!-- Team Overview -->
    <div class="mt-4 p-4 ${isDark ? 'bg-blue-500/10 border-blue-500/20' : 'bg-blue-50 border-blue-100'} rounded-lg border">
      <div class="flex items-center justify-between mb-3">
        <h4 class="font-semibold ${isDark ? 'text-blue-300' : 'text-blue-900'} flex items-center">
          <!-- Icon -->
          Team Assignments
        </h4>
        <button class="p-1 rounded ${isDark ? 'hover:bg-blue-500/20 text-blue-300' : 'hover:bg-blue-100 text-blue-700'}">
          <!-- Icon -->
        </button>
      </div>
      <div class="space-y-3">
        <!-- Loop through assignees -->
        <div class="flex items-center justify-between">
          <div class="flex items-center space-x-2">
            <!-- Avatar component -->
            <span class="text-sm font-medium ${isDark ? 'text-blue-100' : 'text-blue-800'}">{contact.name}</span>
          </div>
          <span class="text-xs ${isDark ? 'text-blue-300 bg-blue-500/30' : 'text-blue-700 bg-blue-100'} px-2 py-1 rounded">
            {taskCount} days
          </span>
        </div>
      </div>
    </div>
  </div>
</div>
src/components/dashboard/RecentActivity.tsx
This component uses a grid layout to display "Upcoming Deals" and "Recent Activity Feed" sections, each with distinct card and list item styling.

Design Code Snippets:


<div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-8">
  <!-- Upcoming Deals -->
  <div class="${isDark ? 'bg-white/5 border-white/10' : 'bg-white border-gray-200'} backdrop-blur-xl border rounded-2xl p-6">
    <div class="flex items-center justify-between mb-6">
      <h3 class="text-lg font-semibold ${isDark ? 'text-white' : 'text-gray-900'}">Upcoming Deals</h3>
      <svg class="w-5 h-5 ${isDark ? 'text-gray-400' : 'text-gray-400'}"><!-- Icon --></svg>
    </div>
    
    <div class="space-y-4">
      <!-- Loop through upcoming deals -->
      <div class="flex items-center justify-between p-4 ${isDark ? 'bg-white/5 hover:bg-white/10' : 'bg-gray-50/80 hover:bg-gray-100/80'} rounded-xl transition-colors">
        <div class="flex items-center space-x-3">
          <!-- Avatar component -->
          <div>
            <h4 class="font-semibold ${isDark ? 'text-white' : 'text-gray-900'} text-sm">{deal.company}</h4>
            <p class="text-xs ${isDark ? 'text-gray-400' : 'text-gray-600'}">
              {/* Contact name */}
            </p>
          </div>
        </div>
        <div class="text-right">
          <p class="font-semibold ${isDark ? 'text-white' : 'text-gray-900'} text-sm">{deal.value}</p>
          <p class="text-xs ${isDark ? 'text-gray-400' : 'text-gray-600'}">{deal.probability} • {deal.dueDate}</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Recent Activity Feed -->
  <div class="${isDark ? 'bg-white/5 border-white/10' : 'bg-white border-gray-200'} backdrop-blur-xl border rounded-2xl p-6">
    <div class="flex items-center justify-between mb-6">
      <h3 class="text-lg font-semibold ${isDark ? 'text-white' : 'text-gray-900'}">Recent Activity</h3>
      <div class="flex items-center space-x-2">
        <span class="text-sm ${isDark ? 'text-gray-400' : 'text-gray-600'}">24 completed</span>
        <span class="text-sm ${isDark ? 'text-gray-400' : 'text-gray-600'}">•</span>
        <span class="text-sm ${isDark ? 'text-gray-400' : 'text-gray-600'}">8 pending</span>
      </div>
    </div>
    
    <div class="space-y-4">
      <!-- Loop through recent activities -->
      <div class="flex items-start space-x-3 p-3 ${isDark ? 'hover:bg-white/5' : 'hover:bg-gray-50/80'} rounded-lg transition-colors">
        <div class="p-2 rounded-lg ${activity.color} ${isDark ? 'bg-opacity-10' : 'bg-opacity-10'}">
          <!-- Icon -->
        </div>
        <div class="flex-1">
          <h4 class="font-medium ${isDark ? 'text-white' : 'text-gray-900'} text-sm">{activity.title}</h4>
          <p class="text-sm ${isDark ? 'text-gray-400' : 'text-gray-600'}">{activity.description}</p>
          <p class="text-xs ${isDark ? 'text-gray-500' : 'text-gray-500'} mt-1">{activity.time}</p>
        </div>
      </div>
    </div>
  </div>
</div>