import React from 'react';
import ReasoningContentGenerator from './ReasoningContentGenerator';

const ReasoningEmailGenerator: React.FC = () => {
  return <ReasoningContentGenerator contentType="email" />;
};

export default ReasoningEmailGenerator;