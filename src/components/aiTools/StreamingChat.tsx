import React, { FC } from 'react';interface StreamingChatProps {  systemPrompt?: string;  initialMessage?: string;  placeholder?: string;  modelOptions?: { value: string; label: string }[];}const StreamingChat: FC<StreamingChatProps> = () => {  return (    <div className="p-4 bg-white rounded-lg shadow">      <p className="text-gray-600">Streaming Chat component is temporarily disabled for deployment.</p>    </div>  );};export default StreamingChat;